import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

//TODO: add test logic here and in the other components

// test('renders home button when location is not overview', () => {
// });

// test('renders overview component when location is overview', () => {
// });

// test('renders accommodation component when location is not overview', () => {
// });

// test('calls checkInAccommodation when handling a checkin with valid data', () => {
// });

// test('updates the system status text when handling a checkin with valid data', () => {
// });

// test('updates the system status text when handling a checkin with valid data and API is not responding', () => {
// });

// test('does not call checkInAccommodation when handling a checkin with valid data, but the person is already checked in somewhere', () => {
// });

// test('updates the system status text when handling a checkin with valid data, but the person is already checked in somewhere', () => {
// });


